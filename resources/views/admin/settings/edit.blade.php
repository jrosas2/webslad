<x-layouts::app title="Configuración">
    <form method="POST" action="{{ route('admin.settings.update') }}" class="mx-auto w-full max-w-4xl space-y-7">@csrf @method('PUT')<div><p class="text-sm font-semibold text-blue-700">Sitio</p><h1 class="mt-1 text-3xl font-bold">Configuración global</h1><p class="mt-2 text-sm text-slate-600">Datos compartidos por el encabezado, pie y contacto.</p></div>@if(session('status'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-800">{{ session('status') }}</div>@endif @foreach($settingGroups as $group => $groupSettings)<section class="legal-card p-6"><h2 class="text-xl font-semibold capitalize">{{ $group }}</h2><div class="mt-5 grid gap-5 sm:grid-cols-2">@foreach($groupSettings as $setting)<div @class(['sm:col-span-2' => in_array($setting->key, ['site_tagline', 'copyright', 'footer_note'], true)])><label class="text-sm font-medium" for="setting-{{ $setting->id }}">{{ $setting->label }}</label><input id="setting-{{ $setting->id }}" name="settings[{{ $setting->id }}]" value="{{ old("settings.{$setting->id}", $setting->value) }}" class="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5"></div>@endforeach</div></section>@endforeach<div class="flex justify-end"><button type="submit" class="primary-button">Guardar configuración</button></div></form>
</x-layouts::app>
