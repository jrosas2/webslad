<x-layouts::app title="Solicitudes de contacto">
    <div class="mx-auto w-full max-w-6xl space-y-7">
        <div><p class="text-sm font-semibold text-blue-700">Comercial</p><h1 class="mt-1 text-3xl font-bold">Solicitudes de contacto</h1><p class="mt-2 text-sm text-slate-600">Consulte, filtre y dé seguimiento a las demostraciones solicitadas.</p></div>
        <form method="GET" class="legal-card grid gap-4 p-5 sm:grid-cols-4"><div class="sm:col-span-2"><label for="search" class="text-sm font-medium">Buscar</label><input id="search" name="search" value="{{ request('search') }}" class="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5" placeholder="Nombre, empresa o correo"></div><div><label for="status" class="text-sm font-medium">Estado</label><select id="status" name="status" class="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5"><option value="">Todos</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ ucfirst(strtolower($status->value)) }}</option>@endforeach</select></div><div><label for="direction" class="text-sm font-medium">Orden</label><select id="direction" name="direction" class="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5"><option value="desc" @selected(request('direction', 'desc') === 'desc')>Más recientes</option><option value="asc" @selected(request('direction') === 'asc')>Más antiguas</option></select></div><div class="flex gap-3 sm:col-span-4"><button class="primary-button" type="submit">Aplicar filtros</button><a href="{{ route('admin.contacts.index') }}" class="secondary-button">Limpiar</a></div></form>
        <section class="legal-card overflow-hidden"><div class="overflow-x-auto"><table class="w-full min-w-[850px] text-left text-sm"><thead class="bg-slate-50 text-xs font-semibold text-slate-500"><tr><th class="p-4">Fecha</th><th class="p-4">Nombre</th><th class="p-4">Empresa</th><th class="p-4">Correo</th><th class="p-4">Teléfono</th><th class="p-4">Estado</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($submissions as $submission)<tr class="hover:bg-slate-50"><td class="p-4 text-xs text-slate-500">{{ $submission->created_at->format('d/m/Y H:i') }}</td><td class="p-4"><a href="{{ route('admin.contacts.show', $submission) }}" class="font-semibold text-blue-700">{{ $submission->name }}</a></td><td class="p-4">{{ $submission->company }}</td><td class="p-4"><a href="mailto:{{ $submission->email }}" class="text-blue-700">{{ $submission->email }}</a></td><td class="p-4">{{ $submission->phone ?: '—' }}</td><td class="p-4"><span @class(['rounded-full px-3 py-1 text-xs font-semibold', 'bg-blue-50 text-blue-700' => $submission->status->value === 'NUEVO', 'bg-amber-50 text-amber-700' => $submission->status->value === 'CONTACTADO', 'bg-emerald-50 text-emerald-700' => $submission->status->value === 'CERRADO'])>{{ $submission->status->value }}</span></td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-slate-500">No se encontraron solicitudes.</td></tr>@endforelse</tbody></table></div></section>
        {{ $submissions->links() }}
    </div>
</x-layouts::app>
