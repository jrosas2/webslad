<x-layouts::app title="Multimedia"><div class="mx-auto w-full max-w-6xl space-y-8"><div><p class="text-sm font-semibold text-blue-700">Biblioteca</p><h1 class="mt-1 text-3xl font-bold">Multimedia</h1><p class="mt-2 text-sm text-slate-600">Cargue imágenes seguras para SEO o secciones administrables.</p></div>@if(session('status'))<div class="rounded-lg border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-800">{{ session('status') }}</div>@endif@if(session('error'))<div class="rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-800">{{ session('error') }}</div>@endif<section class="legal-card p-6"><form method="POST" action="{{ route('admin.media.store') }}" enctype="multipart/form-data" class="grid items-end gap-5 sm:grid-cols-3">@csrf<div><label for="image" class="text-sm font-medium">Imagen</label><input id="image" type="file" name="image" required accept="image/jpeg,image/png,image/webp" class="mt-2 block w-full text-sm"></div><div><label for="alt_text" class="text-sm font-medium">Texto alternativo</label><input id="alt_text" name="alt_text" value="{{ old('alt_text') }}" class="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5"></div><button type="submit" class="primary-button">Cargar imagen</button></form>@error('image')<p class="mt-3 text-sm text-red-600">{{ $message }}</p>@enderror</section><div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">@forelse($mediaItems as $media)<article class="legal-card overflow-hidden"><img src="{{ $media->url() }}" alt="{{ $media->alt_text }}" class="aspect-video w-full object-cover" loading="lazy"><div class="p-4"><p class="truncate text-sm font-semibold">{{ $media->filename }}</p><p class="mt-1 text-xs text-slate-500">{{ Number::fileSize($media->size) }} · {{ $media->mime_type }}</p><code class="mt-3 block rounded bg-slate-100 p-2 text-[11px]">{{ $media->path }}</code><form method="POST" action="{{ route('admin.media.destroy', $media) }}" class="mt-4" onsubmit="return confirm('¿Eliminar esta imagen?');">@csrf @method('DELETE')<button type="submit" class="text-sm font-semibold text-red-600 hover:text-red-700">Eliminar</button></form></div></article>@empty<p class="text-sm text-slate-500">No hay imágenes cargadas.</p>@endforelse</div>{{ $mediaItems->links() }}</div></x-layouts::app>
